    /**
     * @return \WhiteOctober\BreadcrumbsBundle\Model\Breadcrumbs
     */
    protected function initBreadcrumb()
    {
        $listUrl = $this->generateUrl('{{ route_prefix }}.list');

        return $this->getBreadcrumbs()
            ->addItem('{{ entity }} List', $listUrl);
    }