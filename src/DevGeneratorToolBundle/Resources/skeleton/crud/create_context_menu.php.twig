
    /**
     * @param null|Entity\Currency $entity
     * @return \Knp\Menu\ItemInterface
     */
    private function createContextMenu(Entity\{{ entity }} $entity = null)
    {
        $menuBuilder = $this->get('menu.context.builder');

        $contextMenu = $menuBuilder->create('{{ entity_name }}', $entity);

        return $menuBuilder->init($contextMenu);
    }
